pipeline {
    agent any
    stages {
        stage('Composer Install') {
            steps {
             sh 'sudo apt-get install curl php-cli php-mbstring git unzip'
            }
        }
    }
}